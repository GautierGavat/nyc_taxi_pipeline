name: 'nyc_taxi_project'
version: '1.0.0'

profile: 'nyc_taxi_project'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  nyc_taxi_project:      # ← ce niveau est obligatoire
    staging:
      +schema: STAGING
      +materialized: view
      +tags: ["staging"]

    intermediate:
      +schema: INTERMEDIATE
      +materialized: ephemeral
      +tags: ["intermediate"]

    marts:
      +schema: MARTS
      +materialized: table
      +tags: ["mart"]         