version: 2

models:
  - name: mart_zone_analysis
    columns:
      - name: pickup_location_id
        tests:
          - not_null
      - name: total_revenue
        tests:
          - not_null

  - name: mart_hourly_patterns
    columns:
      - name: pickup_hour
        tests:
          - not_null
      # On a enlevé le test unique ici car l'heure se répète (Weekend vs Weekday)